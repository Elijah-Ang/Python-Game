# Pandas: Clean & Transform

**Concepts:**
- Create new columns
- Fix types with `.astype()`
- Handle missing values (basic)

**Why it matters:** Cleaning turns raw data into analysis-ready data.

## Steps
1. Compute revenue = units * price.
2. Create a new column.
3. Print summary.

## Try it
Using the sales df, create `revenue = units * price`.
Print the total revenue (sum of the revenue column).
