# Pandas: Load Data

**Concepts:**
- Read CSV into a DataFrame
- Inspect `.head()` and `.shape`

**Why it matters:** Pandas is the backbone of real-world data wrangling.

## Steps
1. Import pandas.
2. Read a CSV.
3. Inspect basic properties.

## Try it
Load `./assets/datasets/sales_tiny.csv` into a DataFrame named `df`.
Print `df.head()` and `df.shape`.
